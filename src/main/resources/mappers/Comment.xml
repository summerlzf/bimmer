<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<!-- "namespace" must be the path of interface, or show error “is not known to the MapperRegistry” when launch -->
<mapper namespace="com.kedu.bimmer.dao.CommentDAO">

    <select id="query" resultType="com.kedu.bimmer.model.Comment">
        <![CDATA[
            select * from Comment where articleId = #{articleId} order by createTime desc
        ]]>
    </select>

    <insert id="insert" parameterType="com.kedu.bimmer.model.Comment">
        <![CDATA[
            insert into Comment(commentId, articleId, userId, content, hidden, createTime)
            values(#{commentId}, #{articleId}, #{userId}, #{content}, #{hidden}, #{createTime})
        ]]>
    </insert>

</mapper>